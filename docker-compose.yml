services:
  jekyll:
    build: .
    container_name: paclarz.github.io
    ports:
      - "4000:4000"
    restart: unless-stopped
    develop:
      watch:
        - action: sync
          path: ./
          target: /app/jekyll
